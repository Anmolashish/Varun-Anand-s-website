/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://www.varunanand.in", // Replace with your website URL
  generateRobotsTxt: true, // Automatically generate a robots.txt file
  sitemapSize: 7000, // Optional: Limit URLs per sitemap file (if needed)
};
